/** Print resolution assumed when translating inches into pixels. */
export const OUTPUT_DPI = 300;

/** Total pixel-area budget — larger requests are proportionally downscaled. */
export const MAX_PIXELS = 8_500_000;

/** Hard cap on the longest edge of a render target. */
export const MAX_SIDE = 4096;
